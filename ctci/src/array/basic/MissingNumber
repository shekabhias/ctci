// Note that the size of the array is n-1
int missingNumber(int n, int arr[]) {
    int sum = (n * (n+1) )/2;
    int arr_sum = 0;
    for (int i : arr){
        arr_sum+=i;
    }
    return sum - arr_sum;
}